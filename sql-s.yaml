apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: default
spec:
  selector:
    app: mysql # This should match the labels in the MySQL pod
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
